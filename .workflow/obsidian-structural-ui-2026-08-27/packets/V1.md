# V1 Integration Verification

Objective: 三つのsliceを元の広い目標に対して統合検証する。

Verification: focused tests、`npm run typecheck`、`npm test`、`npm run check:mcp`、`git diff --check`、isolated Electron 1440／900／720、safe production update、fresh runtime／delivery match。

Unverified boundary: physical 125／150／200% DPI、High Contrast、Narrator／NVDA、長時間眼精疲労、利用者受入。
