# UI2 Activity Rail

Objective: Files、Search、Graph、Bookmarks、Commandsを既存handlerのまま細いrailへ再配置する。

Ownership: shell markup、rail component／styles、該当App regression tests。

Do not: search engine、workspace model、FileTree semantics、永続設定を追加しない。

Verification: accessible names、tooltip／title、focus order、sidebar独立開閉、720／900px。
