# P0-7 — 名前変更・移動時の参照元リンクの非破壊追従

Date: 2026-09-05 JST. Owner selected this repair after P0-6. This report fixes the source acceptance boundary before the production snapshot. The latest production receipt and final Vault campaign own the subsequent installation result; do not edit this file just to duplicate a successful receipt.

Human single-Markdown-note rename / move now follows unambiguous incoming Wiki and local Markdown links. Only destination spans change. Alias labels, heading fragments, frontmatter comments, number spelling, BOM and line endings remain unchanged. This closes the selected P0-6 stale-reference case while retaining TSUZUNE's non-destructive storage behavior.

## Implementation and safety

- `src/core/move-links.ts`: existing CommonMark parser and Wiki index; no new dependency or YAML serialization. Skip code, comments, external URL spans and ambiguous links. Use explicit Vault Wiki paths and encoded relative Markdown destinations.
- `src/main/entry-move.ts`: existing preflight fingerprint includes affected reference revisions. Existing durable move journal retains before/after text; revision-checked saves and recovery roll back interrupted updates. Unknown external edits retain the journal and require recovery rather than overwriting user data. AI/MCP move semantics remain unchanged.
- `src/renderer/App.tsx`: confirmation describes incoming-link following; a clean, already-open reference refreshes after a path operation. Dirty editor state retains the existing conflict boundary.
- `src/main/index.ts` and packaged startup check: an explicit absolute `--user-data-dir` sets both Electron `userData` and `sessionData` before readiness. Smoke readiness reports both live paths. Normal startup without the flag retains existing paths.

Protected Inbox / raw sources / legacy history are not rewritten, including a moved note's self-reference if either its original or new location is protected. Invalid UTF-8 remains byte-identical. Affected Wiki references in YAML block scalars stop the move before any write; unsafe Wiki destination delimiters and apostrophes inside single-quoted YAML values also stop before mutation.

## Verified source boundary

| Evidence | Result |
|---|---|
| Test-first human link-following regression | RED on unchanged references, GREEN after span edits |
| Move, rollback, stale revision, external edit, protected source, URL and YAML edges | `tests/entry-move.test.ts`: 32/32 PASS |
| Real child-process exits | prepared / filesystem / partial links roll back; committed ledger retains complete updates; includes self-links |
| Build and typecheck | PASS |
| Independent safety review | gpt-5.6-sol / high: 32/32 and typecheck PASS; no unresolved blocking findings |
| Profile cause audit | gpt-5.6-terra / high, read-only: P0-6 cause remains unproven; explicit live profile/session proof required |
| Complexity review | Existing parser, index, journal and save path reused; no new dependency, runtime or abstraction required |
| Headed current-source fixture | All four P0-6 UI operations have exact expected bytes for all 10 data files; fresh process preserves every byte |
| Source fixture isolation | Live userData/sessionData and Vault equal explicit fixture paths; production profile 273 files byte-identical before/after |

Headed evidence: `output/playwright/p0-7-link-maintenance-20260905/source-verified/verification.json`, `isolation.json`, before/after profile manifests, data manifest and screenshots. The harness waits for Vault restoration before asserting identity. Two earlier harness attempts read before restoration and stopped with NO_VAULT; no product operation occurred. They are not acceptance runs.

The first full gate stopped before installation on one existing UI assertion: the revised confirmation omitted the referring-note count. The count was restored without changing link behavior; the focused existing UI test passed. The full production gate must rerun from the final source snapshot. Initial failed-gate evidence is retained in `output/playwright/p0-7-link-maintenance-20260905/production-update.log`.

## Production delivery and evidence ownership

The exact approved source manifest from the preceding whole-tree promotion plus the P0-6 documentation delta and P0-7 task-owned changes defines the promotion boundary. `output/playwright/p0-7-link-maintenance-20260905/source-delta.json` must contain no unexpected path. User changes remain intact; there is no Git publication.

Run the official `npm run production:update`: typecheck, complete production test suite, MCP checks, package, installer inspection, packaged smoke, installed smoke, EXE / app.asar equality, production profile equality, unchanged source fingerprint and MCP re-registration. Installation acceptance is proven only by the new `docs/reports/production-update-latest.json` receipt for this source fingerprint. Then run the same headed fixture against the installed executable; its evidence is `output/playwright/p0-7-link-maintenance-20260905/installed/verification.json`. The final existing Vault campaign records both results and source delivery status. A failed gate or failed installed fixture leaves the slice incomplete.

## Deliberate boundaries

This does not establish complete Obsidian parity. P0-6 is the fixed Obsidian 1.13.4 comparison; P0-7 replays the same anonymous input and operations against the repaired TSUZUNE. New exact-byte results differ intentionally from Obsidian's formatting changes.

- Folder moves, outgoing relative-link preview, query-bearing Markdown destinations and arbitrary YAML forms are outside this slice. Markdown destination text follows the move, but the pre-existing relative-link preview limitation remains.
- The dialog's reference count comes from the Wiki index; Markdown-only references can follow without appearing in that count.
- Crash checks cover abrupt process exit, not power-loss/filesystem durability. Recovery retains externally changed notes and requires intervention.
- P0-6's ten cache/session differences remain an unexplained historical failure. P0-7's explicit live isolation and fresh invariant proof do not retroactively turn P0-6 into PASS.

Current next step: finish the authorized delivery and final campaign synchronization for this slice. Further compatibility work requires a separate selection; no Held or Research feature is restarted here.
