# W1 — 現行Vault構造の読取結果

## 件数

| 区分 | ノート数 |
|---|---:|
| `00_入口` | 11 |
| `01_受信箱` | 1 |
| `02_デイリー` | 2 |
| `10_プロジェクト` | 11 |
| `20_分野` | 6 |
| `30_知識` | 412 |
| `40_情報源` | 70 |
| `50_履歴` | 8（内容は未読） |
| `90_テンプレート` | 3 |
| root `knowledge.md` | 1（Freebuff管理、対象外） |

調査時点のTSUZUNE管理対象は516件。root `knowledge.md` 1件と旧履歴8件を含むVault内Markdown総数は525件。管理対象の79.8%が `30_知識` にある。ただし件数は実行開始時に再集計し、516を固定前提にしない。

## 現在の入口

- 人間向け: `00_入口/ホーム.md`、`現在地.md`、プロジェクト地図、知識地図、分野地図、情報源・履歴地図。
- AIも人間と同じ `00_入口/ホーム.md` と各地図を使う。AI専用の入口は現時点で不要。
- 知識側には既に6つのカテゴリMOCがあり、物理移動よりも所属リンクの監査を先に行える。

## 主な摩擦

- 問題はフォルダ不足ではなく、必要な文脈の読む順、現行判断と証拠の区別、複数責務ノートである。`30_知識` の件数だけを理由に全件意味監査は行わない。
- `00_入口/ホーム.md` と `00_入口/情報源・履歴地図.md` は、履歴廃止後の表現と一致していない。
- root `knowledge.md` はFreebuffのAgents.mdであり、TSUZUNEのAI arrival / MyContextとして扱ってはならない。

## 未確認境界

- 全件の孤立ノート数、破損リンク数、6カテゴリMOCへの到達性は未監査。まず機械的に確認し、例外だけを内容監査する。
- ノート名とサイズだけでは分割可否を決められない。原典・対話記録・スナップショットは長くても分割対象とは限らない。
- root `knowledge.md` は今後の内容監査、整理判定、更新対象から除外し、未変更hashだけを確認する。
- 本調査では本番Vaultへ作成・更新・移動・削除を行っていない。
