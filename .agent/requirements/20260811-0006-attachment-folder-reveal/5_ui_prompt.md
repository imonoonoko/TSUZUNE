# UI Prompt — GP0-3b-o

Design the next Graph context-menu parity slice for an existing attachment. Preserve the current menu, Graph state, and trusted path safety. First observe fixed Obsidian 1.13.4; do not assume that `フォルダで表示` means the same thing as `ファイルエクスプローラでファイルを表示`. Do not launch Explorer during capture. If implementation is later approved, expose one attachment-only action with a concise error path and no new generic navigation system.
