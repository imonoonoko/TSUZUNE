# Packet A — contributor ledger and persistent world

Implement the deterministic data/model slice defined in `plan.md` and `protocol.json`. Ownership and acceptance are supplied in the agent assignment. Result goes to `results/A-contributor-world.md`.
