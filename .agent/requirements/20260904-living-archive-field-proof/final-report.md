# Living Archive Field — final report

## Baseline correction — 2026-09-05

The workshop owner identified Gate 3B.5 as the exact earlier cosmic artwork. The restored Living Archive Field revision was therefore still the wrong baseline. The active in-app-browser surface now points to the intact Gate 3B.5 implementation at `work/archive-weather-prototype/`; no renderer code was copied or rewritten.

The live Gate 3B.5 page restores the white 599-note star field, shared flow history, changing foreground/background depth, continuous event composition, and cut-free observation camera. Its 38 focused tests and syntax check pass, and the live WebGL2 surface reports error 0. This is a restored viewing baseline, not a new aesthetic acceptance or production promotion.

## Correction update — 2026-09-05

The workshop owner first rejected the live presentation as visually stationary, then correctly rejected the amplified correction as substantially worse than the preceding artwork. The first response combined a necessary clock repair with unnecessary increases to camera travel, filament displacement, membrane deformation, and witness drift. That destroyed the quiet negative space and stable depth that had made the earlier version stronger.

The isolated candidate now preserves only the root clock repair: sparse visible frames no longer discard elapsed wall time. The preceding camera and shader values are restored exactly, so energy again travels primarily as light over restrained geometry. The same-page visual-difference gate is bounded on both sides so it catches both a frozen presentation and another over-amplified regression.

The restored revision passes its focused tests and short live acceptance and is ready for owner rereview. Its earlier twelve-minute technical run remains historical evidence for an older revision, not a current-revision acceptance claim.

## Outcome

The isolated artwork candidate is implemented, the wall-time defect is corrected, and the aesthetic overcorrection has been removed. It remains one persistent world: archive-derived energy moves as travelling light on world-space filaments, passes through translucent membranes, gathers at witness clusters, and remains coherent while a slowly continuous camera observes it.

The proof is ready for workshop-owner rereview. It is not yet aesthetically accepted, and the current visual revision has not repeated the full twelve-minute technical run.

## Changed artifacts

- Artwork and tests: `work/living-archive-field-prototype/`
- Frozen contract and evidence: `.agent/requirements/20260904-living-archive-field-proof/`
- Existing `work/archive-weather-prototype`, product `src`, package metadata, dependencies, production installation, and Git state were not changed for this proof.

## Verification

- Current restored revision: all 24 deterministic/model/renderer/harness tests and all five syntax checks pass.
- Current restored revision: the live Codex browser advanced 2.00 simulation seconds over 2.214 wall seconds with reduced motion false; four-second before/after frames showed the light moving along stable filaments without the rejected shape lurch.
- Current restored revision: the eight-second Electron smoke passed its focused boundary, bounded live-motion, reduced-motion, context-loss, frame, and console-error checks. Its three-second mean pixel difference was `0.014463`, within the `0.008–0.035` regression band; the owner-rejected amplified revision measured `0.045718`.
- Historical pre-correction revision: one no-retry, actual-time twelve-minute Electron run passed its then-frozen technical invariants. Because renderer and camera behavior changed afterward, that run is not claimed as current-revision acceptance.
- Independent read-only audits recovered the exact pre-amplification values and checked the motion/test boundary. The parent restored the rendering, retained the clock repair, and repeated an unseen live-browser check.

## What the evidence does not prove

- It does not answer whether this is beautiful, whether the archive causality is felt without explanation, or whether the work deserves repeated viewing.
- It does not complete the anonymous A/B/C owner comparison.
- It does not establish note value, a true semantic relation, or an ontology. Field, flow, depth, and material are local artistic representations.
- It does not authorize or perform product integration, dependency changes, production update, or Git delivery.

## Owner gate

Judge the live candidate without opening the provenance/control evidence first:

1. Did my archive cause this?
2. Do I want to keep watching for ninety seconds?
3. Do I want to revisit on another day?

Only three yes answers may promote the artwork from technically verified candidate to aesthetically accepted direction.

## Residual issue

`protocol.json` names its deterministic score-reference clock `source.archiveTimeUtc`, while the snapshot's actual observation time is earlier. The execution evidence preserves both values and does not reinterpret the protocol field as source provenance. Rename or derive the field only in a future protocol round.

The persisted report keeps performance aggregates rather than all 161,553 raw frame records, so later reviewers cannot recompute its percentiles from the report alone. Camera limits also remain duplicated between protocol and model. Neither invalidated this frozen run; both are explicit hardening items for a future round, not silent post-acceptance edits.
