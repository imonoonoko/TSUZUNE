# CP0-T02 目的

Obsidian Desktop 1.13.4 と TSUZUNE の Global Graph `Search files` に、入力途中または無効な7クエリを同じ順序・同じfixtureで与え、入力値、表示node集合、エラー表示、Graph再表示、アプリ再起動後の差を固定証拠にする。

このtaskは差の有無を一件だけ閉じる。差がある場合も、既存`graph-query` parserの局所修正で閉じないなら実装を拡張しない。
