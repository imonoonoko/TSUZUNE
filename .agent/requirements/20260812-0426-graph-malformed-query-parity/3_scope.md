# CP0-T02 Scope

## In

- `fixtures/obsidian-graph-parity-vault` の読み取り専用コピー
- 固定7 query matrix
- Obsidian 1.13.4 とTSUZUNE working-tree buildの隔離・画面外capture
- query、node IDs、エラー表示、Graph再表示、アプリ再起動後の比較
- 差が一つのparser境界で閉じる場合だけ、`src/core/graph-query.ts` と既存testの最小修正

## Out

- Excluded files、Groups、index/database変更
- 新規依存、package script、AI、複数surfaceの変更
- foreground GUI、物理入力、通常Vaultの変更
- commit、push、release
