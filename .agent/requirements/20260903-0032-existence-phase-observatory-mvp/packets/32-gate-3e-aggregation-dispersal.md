# Packet 32 — Gate 3E aggregation / dispersal contrast

## Decision

第三案は長期到達像として保持する。直近はGate 3B.5の一場面を壊さず、既存の集合・圧縮・破裂・播種の振幅と可読性を上げる。

## Selected visual truth

`C:\Users\Humin\.codex\generated_images\01a06aef-b41b-7070-af7a-78ed4c30b041\exec-fb8284c5-c5d4-4375-8590-203a26b71824.png`

- 黒い余白が主で、三つの非同期な領域だけが現象を起こす。
- 集合は中心記号ではなく、局所的な吸引、密度上昇、前後差として見える。
- 離散は同心円でなく、方向性を持つ粒子と短い繊維状の残光として見える。
- 恒久的な最亮点や画面全域の模様を作らない。

## Implementation owner

- role: 制作員
- ownership: `work/protostar-field-prototype/` のみ
- forbidden: Gate 3B.5本体、product source、Vault、workflow文書、Git、新依存
- verification owner: CEO-01。source testに加えてin-app browserの実機描画と未提示境界を確認する。

## Acceptance

1. 599ノートが一対一の光点として描かれる。
2. 5〜30秒の連続観察で、少なくとも一つの集合領域と一つの離散領域を同時または連続して識別できる。
3. camera cut、graph line、ring、nebula wash、常設hero orbがない。
4. pause、reduced motion、resizeが破綻せず、GL error 0、console/runtime errorなし。
5. Gate 3B.5のfilesは変更されない。

## Stop

実機比較とdesign QAを終えて工房主へ提示する。Tendrils型循環場や本番統合へ自動で進めない。

## Verified result — 2026-09-05

- `work/protostar-field-prototype/` だけを変更し、局所phaseを各note lightへ渡した。集合は丸く締まり発光を増し、離散は実velocity軸へ沿う細い短線として残る。
- 同一場でcapture + ruptureを同時観測し、診断値は `gather 1.000 / release 0.725 / 599 lights / GL 0 / finite true`。compression + seedingの同時断面も `gather 0.877 / release 0.485` で確認した。
- 参照原寸 `1664 x 960` を含む同一比較入力、5／15／29秒断面、pause、制作情報、consoleを実機確認した。暗い負空間は概ね60%以上を維持した。
- focused source verificationは `28 passed / 0 failed`。Gate 3B.5の10 filesは開始時SHA-256と一致した。
- Evidence: `work/protostar-field-prototype/design-qa.md`。
- stop成立。第三案の長い循環field、製品接続、本番反映、音、Git deliveryは未実施。美的採否は工房主へ戻す。
