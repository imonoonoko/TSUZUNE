# Google Desktop OAuth credentials embedded into the packaged main process.
# Keep actual values out of Git; provide them only while building.
# Never add a refresh token or private account data to this file.
MAIN_VITE_GOOGLE_OAUTH_CLIENT_ID=
MAIN_VITE_GOOGLE_OAUTH_CLIENT_SECRET=
